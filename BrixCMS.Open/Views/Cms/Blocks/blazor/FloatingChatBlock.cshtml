@model BrixCMS.Open.Models.BlazorBlocks.FloatingChatBlock

@* Floating chat button — renders at a fixed screen position, no wrapper needed *@
@(await Html.RenderComponentAsync<BrixCMS.Open.Components.BlazorBlocks.FloatingChatBlockComponent>(
    RenderMode.Server,
    new { Model = Model }))
