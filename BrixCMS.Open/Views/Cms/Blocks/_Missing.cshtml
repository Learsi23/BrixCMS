@* Rendered in place of a block whose type is no longer registered (removed in a later
   version but still referenced by an old page). Renders nothing, so a stale block can
   never 500 the public page — it is simply skipped. *@
