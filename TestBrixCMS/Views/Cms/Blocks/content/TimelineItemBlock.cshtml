@using TestBrixCMS.Models.Blocks
@model TimelineItemBlock

<div class="border-l-4 border-blue-400 pl-4 py-2">
    <p class="font-semibold text-gray-800">@(Model.Title?.Value ?? "")</p>
    <p class="text-sm text-gray-500">@(Model.Description?.Value ?? "")</p>
</div>
